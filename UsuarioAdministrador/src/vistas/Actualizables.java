package vistas;

public interface Actualizables {
    /**
     * Método que llamará el menú lateral cada vez que se haga clic
     * en un botón para forzar una nueva petición al servidor.
     */
    void recargarDatos();
}
